import SearchListItem from "./SearchListItem"

const SearchList = () => {
  return (
    <main className="col-span-2">
        <SearchListItem/>
        <SearchListItem/>
        <SearchListItem/>
        <SearchListItem/>
    </main>
  )
}

export default SearchList
